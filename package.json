{
  "name": "kromio-qa-suite",
  "version": "1.0.0",
  "description": "Independent QA test suite and adversarial audit for Kromio.ai (AI Chrome extension builder)",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
